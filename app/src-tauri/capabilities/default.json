{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the main window: core APIs plus the P0 plugin set (dialog, fs, notification, autostart, process, store). Every permission listed here is one the GUI is allowed to use; anything not listed is denied by default, which is the point of Tauri's capability model.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "fs:default",
    "notification:default",
    "autostart:default",
    "process:default",
    "store:default"
  ]
}
